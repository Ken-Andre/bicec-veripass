# Test override: builds the API image with backend dev dependencies without
# changing the production Dockerfile default.
services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        INSTALL_DEV_DEPS: "true"
