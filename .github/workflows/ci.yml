name: CI Foundation

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

permissions:
  contents: read

jobs:
  lint-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          cd code/backend
          python -m pip install --upgrade pip
          pip install ruff==0.8.6
      - name: Lint with ruff
        run: |
          cd code/backend
          ruff check . --output-format=github

  lint-mobile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: |
          cd code/mobile
          npm install --legacy-peer-deps
      - name: Lint with ESLint
        run: |
          cd code/mobile
          npm run lint

  lint-backoffice:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.10'
      - name: Install dependencies
        run: |
          cd code/backoffice
          bun install --frozen-lockfile
      - name: Lint with ESLint
        run: |
          cd code/backoffice
          bun run lint

  test-backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: veripass_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install uv
        uses: astral-sh/setup-uv@v4
      - name: Install dependencies
        run: cd code/backend && uv sync
      - name: Run tests
        env:
          DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/veripass_test
          REDIS_URL: redis://localhost:6379/0
          JWT_SECRET: ${{ secrets.JWT_SECRET || format('test-secret-{0}', github.run_id) }}
          ENVIRONMENT: testing
          OTP_MODE: dev_local
          STORAGE_PATH: ${{ runner.temp }}/veripass-documents
          MODELS_PATH: ${{ runner.temp }}/veripass-models
        run: cd code/backend && uv run pytest -v

  test-backoffice:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.10'
      - name: Install dependencies
        run: |
          cd code/backoffice
          bun install --frozen-lockfile
      - name: Run tests
        run: |
          cd code/backoffice
          bun run test

  build-mobile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Install dependencies
        run: |
          cd code/mobile
          npm install --legacy-peer-deps
      - name: Build
        run: |
          cd code/mobile
          npm run build

  build-backoffice:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.10'
      - name: Install dependencies
        run: |
          cd code/backoffice
          bun install --frozen-lockfile
      - name: Build
        run: |
          cd code/backoffice
          bun run build
